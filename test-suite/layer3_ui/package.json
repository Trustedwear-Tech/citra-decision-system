{
  "name": "citra-ui-rendering-matrix",
  "private": true,
  "type": "module",
  "description": "Layer 3 — UI rendering matrix: renders every panel type in every data state against a mock API and screenshots each cell.",
  "scripts": {
    "mock": "node mock-api.mjs",
    "test": "playwright test",
    "update-snapshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
