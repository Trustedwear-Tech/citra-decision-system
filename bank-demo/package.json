{
  "name": "acme-bank-demo",
  "private": true,
  "version": "0.1.0",
  "description": "A pretend customer application that embeds the Citra decision card. Next.js, because an API route IS the server-side token exchange a real integrator needs.",
  "scripts": {
    "dev": "next dev -p 4300",
    "build": "next build",
    "start": "next start -p 4300"
  },
  "dependencies": {
    "next": "^15.5.21",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "typescript": "^5.4.5"
  },
  "//overrides": [
    "next pins postcss to an exact version, so patching next never moves it --",
    "it sat at 8.4.31 against advisories fixed in 8.5.23. Same override as",
    "citra-app-runtime; keep the two in step."
  ],
  "overrides": {
    "postcss": "^8.5.28"
  }
}
