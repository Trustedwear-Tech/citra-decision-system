node_modules/
.next/
out/
*.log
.DS_Store

# Embed bundle build output — regenerated by `npm run build:embed`, never committed.
# The Dockerfile builds it in the builder stage and copies public/ into the runner.
public/v1/

# Screenshots produced by the embed E2E runs — evidence for a review, not source.
embed/dev/*.png
e2e/embed-test/
