fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
docker==7.1.0
httpx==0.27.2
python-multipart==0.0.31
# psutil: read total RAM + per-process / system CPU%. The scheduler's
# /capacity endpoint reports free_ram_bytes so the SandboxPool can
# refuse to pick this host for a Heavy spawn that wouldn't fit. Without
# psutil the response sets free_ram_bytes=-1 and capacity tracking
# degrades to slot count only.
psutil==6.1.0
