[pytest]
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    mutating: test writes to the source of record; requires DA_ALLOW_MUTATING=1
    builder: test spawns a builder pod (slow, uses LLM budget); requires DA_RUN_BUILDER=1
