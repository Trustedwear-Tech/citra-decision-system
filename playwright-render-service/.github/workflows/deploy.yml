# Copyright (c) 2026 Trustedwear Tech Private Limited (https://citra-ai.com)
# Author: Rohit Kumar Chandan
# SPDX-License-Identifier: BUSL-1.1
#
# Licensed under the Business Source License 1.1. Non-production use is granted;
# production use requires a commercial licence until the Change Date, after
# which this file converts to Apache-2.0. See LICENSE at the repository root.

name: Build & Deploy Playwright-Render-Service

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        description: 'Target environment'
        options:
          - prod
          - test
        required: true
        default: 'test'

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}
  IMAGE_NAME: playwright-render-service

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push image
        run: |
          IMAGE=${{ env.IMAGE_PREFIX }}/${{ env.IMAGE_NAME }}

          docker build \
            -t ${IMAGE}:${{ inputs.environment }}-${{ github.sha }} \
            -t ${IMAGE}:${{ inputs.environment }}-latest \
            .

          docker push ${IMAGE}:${{ inputs.environment }}-${{ github.sha }}
          docker push ${IMAGE}:${{ inputs.environment }}-latest

  deploy:
    name: Deploy to Server
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        env:
          IMAGE_PREFIX: ${{ env.IMAGE_PREFIX }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          ENVIRONMENT: ${{ inputs.environment }}
          GHCR_PAT: ${{ secrets.GHCR_PAT }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          envs: IMAGE_PREFIX,IMAGE_NAME,ENVIRONMENT,GHCR_PAT
          command_timeout: 5m
          script: |
            set -e

            IMAGE="${IMAGE_PREFIX}/${IMAGE_NAME}"
            ENV="${ENVIRONMENT}"
            SERVICE_DIR="$HOME/Citra-AI/playwright-render-service"

            echo "=== Deploying Playwright-Render-Service ($ENV) ==="

            # Login to GHCR
            echo "${GHCR_PAT}" | docker login ghcr.io -u github --password-stdin

            # Pull new image
            docker pull ${IMAGE}:${ENV}-latest

            # Retag to local name that docker-compose expects
            docker tag ${IMAGE}:${ENV}-latest playwright-render-service:latest

            cd ${SERVICE_DIR}

            # Restart via docker compose
            docker compose up -d --no-build --force-recreate

            # Health check
            sleep 10
            curl -fsS http://127.0.0.1:3001/health || echo "WARNING: health check failed"

            # Cleanup
            docker image prune -f

            echo "=== Deploy complete ==="
